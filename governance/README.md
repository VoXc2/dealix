# Governance Layer

This directory centralizes execution governance for Revenue OS operations.

Core enforcement flow:

`risk score -> policy check -> approval check -> execution -> audit log`

Primary matrix reference:

- `platform/governance/action_risk_matrix.md`
