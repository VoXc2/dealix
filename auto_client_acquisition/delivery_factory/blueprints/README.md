# Service Blueprint Library

Blueprints for core Dealix service lines. Link each blueprint to `service_id` in commercial readiness.

| service_id | blueprint |
| --- | --- |
| revenue_os_core | delivery + governance gates |
| pilot_499 | 7-day sprint + proof target |
| data_pack | import preview + DQ score |

Add YAML per service as engagements mature.
