"""Draft snippets — never asserts revenue without evidence."""
from __future__ import annotations


def build_snippet_ar(theme: str) -> dict[str, str]:
    return {
        "snippet_ar": f"تجربة تشغيلية حول {theme} — بدون أرقام إيراد إلا مع مرجع دفع.",
        "disclaimer_ar": "مسودة فقط؛ تحتاج موافقة كتابية قبل النشر.",
    }
