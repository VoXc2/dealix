name: Brain Control Command

on:
  workflow_dispatch:
    inputs:
      command:
        description: "Command to send to Dealix Brain"
        required: true
        type: string
      priority:
        description: "Priority"
        required: false
        default: "normal"
        type: choice
        options:
          - low
          - normal
          - high

permissions:
  contents: read

jobs:
  brain-control:
    runs-on: ubuntu-latest
    env:
      AUTO_SEND_ENABLED: "false"
      EXTERNAL_OUTREACH_ENABLED: "false"
      AGENT_APPROVAL_MODE: "required"
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
      - name: Run brain command
        env:
          DEALIX_BRAIN_COMMAND: ${{ inputs.command }}
          DEALIX_BRAIN_PRIORITY: ${{ inputs.priority }}
        run: |
          python scripts/dealix_brain_control.py ask "$DEALIX_BRAIN_COMMAND" --priority "$DEALIX_BRAIN_PRIORITY"
          python scripts/dealix_brain_control.py tick
          python scripts/dealix_brain_control.py doctor || true
      - name: Upload brain response
        uses: actions/upload-artifact@v7
        with:
          name: brain-control-response
          path: |
            reports/company_os/control/**
            data/company_os/control/**
