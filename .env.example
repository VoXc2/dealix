# Dealix Environment Configuration | إعدادات بيئة Dealix
# Copy to `.env` and fill in real values. NEVER commit `.env`.
# Generate secrets with: python -c "import secrets; print(secrets.token_hex(32))"

# ============================================================
# APP / ENVIRONMENT
# ============================================================
APP_ENV=development            # development | staging | production | test
APP_DEBUG=false
APP_HOST=0.0.0.0
APP_PORT=8000
APP_URL=http://localhost:8000
APP_TIMEZONE=Asia/Riyadh
APP_DEFAULT_LOCALE=ar
APP_DEFAULT_CURRENCY=SAR
APP_LOG_LEVEL=INFO             # DEBUG | INFO | WARNING | ERROR | CRITICAL

# Backend app secret — generate a real 64-byte hex value in prod.
APP_SECRET_KEY=change-me-in-production

# Legacy env keys (referenced by scripts/check_env_contract.py and
# scripts/dealix_customer_credentials_check.py). APP_ENV / APP_LOG_LEVEL
# above are the canonical pydantic-settings keys; these mirror them.
ENVIRONMENT=development
LOG_LEVEL=INFO

# Comma-separated CORS origins for the frontend.
CORS_ORIGINS=http://localhost:3000,http://localhost:8000

# Git SHA surfaced on /health (auto-set by Dockerfile/Railway).
GIT_SHA=unknown

# Public base URLs (used by frontend, email links, webhooks).
BASE_URL=http://localhost:8000
DEALIX_API_BASE=http://localhost:8000

# ============================================================
# OUTBOUND SAFETY — all disabled by default. Never enable in .env.example.
# ============================================================
EXTERNAL_SEND_ENABLED=false
OUTBOUND_MODE=draft_only
EMAIL_SEND_ENABLED=false
WHATSAPP_SEND_ENABLED=false
WHATSAPP_ALLOW_LIVE_SEND=false
SMS_SEND_ENABLED=false

# ============================================================
# AUTH / JWT  (required in production — startup fails otherwise)
# ============================================================
# 32+ char hex. Generate: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET_KEY=change-me-jwt-32-byte-hex-here
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30
JWT_REFRESH_TOKEN_EXPIRE_DAYS=30
JWT_INVITE_TOKEN_EXPIRE_HOURS=72

# Comma-separated client API keys for /api/v1/* (non-admin).
API_KEYS=
# Comma-separated admin API keys for /api/v1/admin/*.
ADMIN_API_KEYS=

# ============================================================
# DATABASES
# ============================================================
# Postgres in prod. Railway/Heroku `postgres://…` is auto-normalized to asyncpg.
DATABASE_URL=postgresql+asyncpg://ai_user:ai_password@localhost:5432/ai_company
REDIS_URL=redis://localhost:6379/0
MONGODB_URI=mongodb://localhost:27017/ai_company

# Proof / Value / Operational ledgers backend selection.
PROOF_LEDGER_BACKEND=file          # file | postgres
VALUE_LEDGER_BACKEND=jsonl         # jsonl | postgres | dual
DEALIX_OPERATIONAL_STREAM_BACKEND=off   # off | postgres | dual
# Orchestrator state: memory (local), json (single node), postgres (multi-node).
# Production fails closed unless the resolved backend is postgres.
DEALIX_ORCHESTRATOR_BACKEND=
DEALIX_ORCHESTRATOR_DATABASE_URL=  # empty reuses DATABASE_URL
# Optional restart-safe JSON prefix for non-production single-node deployments.
DEALIX_ORCHESTRATOR_STATE_PATH=
OTEL_CONTRACT_TRACE_EXPORT=false

# ============================================================
# LLM PROVIDERS
# ============================================================
# Provider credentials below are compatibility inputs only. Unattended Dealix
# execution must resolve its final model through the canonical Company Machine
# model/cost/data broker and Session Factory. Provider defaults, worker env and
# this example file are NEVER automatic model or cost authority.
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-5-20250929
ANTHROPIC_MAX_TOKENS=4096
ANTHROPIC_TIMEOUT=60

# Legacy OpenRouter gear variables are model-neutral compatibility handles.
# Leave them blank by default. They must never bypass the canonical broker.
GEAR1_MODEL=
GEAR2_MODEL=
GEAR3_MODEL=

# Optional compatibility providers.
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o-mini
# DeepSeek keys are retained only for explicit manual/provenance compatibility.
# Founder unattended policy is NO_DEEPSEEK: never treat these values as
# automatic routing, fallback, paid-spill or provider-authority evidence.
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com/v1
GLM_API_KEY=
GLM_BASE_URL=https://open.bigmodel.cn/api/paas/v4
GOOGLE_API_KEY=
GEMINI_MODEL=gemini-1.5-pro
GROQ_API_KEY=
GROQ_BASE_URL=https://api.groq.com/openai/v1

# ============================================================
# SAUDI INTEGRATIONS
# ============================================================
# Moyasar payments — https://moyasar.com/dashboard
# MOYASAR_SECRET_KEY is the HTTP Basic username (secret key).
# MOYASAR_WEBHOOK_SECRET validates signed webhooks (constant-time compare).
MOYASAR_API_KEY=pk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxx
MOYASAR_SECRET_KEY=sk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxx
MOYASAR_WEBHOOK_SECRET=
MOYASAR_LIVE_MODE=false

# ZATCA e-invoice Phase 2 — https://zatca.gov.sa
ZATCA_CSID=
ZATCA_SECRET=
ZATCA_SANDBOX=true
ZATCA_SELLER_NAME=
ZATCA_SELLER_VAT=
ZATCA_SELLER_CRN=
ZATCA_SELLER_CITY=Riyadh
ZATCA_SELLER_STREET=
ZATCA_SELLER_POSTAL=

# WhatsApp — canonical production target: Meta Cloud API.
# GREEN-API is transitional only while the official Meta sender is activated.
# Transport configuration is never consent authority.
WHATSAPP_PROVIDER_PREFERENCE=auto   # auto | meta_cloud | green_api | ultramsg | fonnte

# GREEN-API transition transport.
GREEN_API_INSTANCE_ID=
GREEN_API_TOKEN=
# Generate an independent random token. Configure the same value as GREEN-API
# Webhook URL Token. Never reuse GREEN_API_TOKEN here.
GREEN_API_WEBHOOK_TOKEN=

# Meta WhatsApp Cloud API — canonical long-term production path.
# https://developers.facebook.com/docs/whatsapp/cloud-api
WHATSAPP_ACCESS_TOKEN=
WHATSAPP_PHONE_NUMBER_ID=
WHATSAPP_BUSINESS_ACCOUNT_ID=
WHATSAPP_VERIFY_TOKEN=
WHATSAPP_APP_SECRET=
# WHATSAPP_ALLOW_LIVE_SEND is set in the OUTBOUND SAFETY section above.

# ============================================================
# EMAIL  (provider set via EMAIL_PROVIDER)
# ============================================================
EMAIL_PROVIDER=resend           # resend | sendgrid | smtp
EMAIL_FROM=noreply@dealix.me
EMAIL_FROM_NAME=Dealix
DEALIX_FOUNDER_EMAIL=founder@dealix.me
RESEND_API_KEY=
SENDGRID_API_KEY=
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=founder@dealix.me
SMTP_PASSWORD=
SMTP_TLS=true

# ============================================================
# LEAD RESEARCH / CRM / CALENDAR
# ============================================================
GOOGLE_MAPS_API_KEY=
HUBSPOT_ACCESS_TOKEN=
HUBSPOT_PORTAL_ID=
GOOGLE_CALENDAR_CREDENTIALS_FILE=
GOOGLE_CALENDAR_ID=primary
CALENDLY_API_TOKEN=
CALENDLY_USER_URI=

# ============================================================
# AUTOMATION
# ============================================================
N8N_WEBHOOK_URL=
N8N_ENCRYPTION_KEY=

# ============================================================
# OBSERVABILITY
# ============================================================
SENTRY_DSN=
SENTRY_ENVIRONMENT=production
SENTRY_TRACES_SAMPLE_RATE=0.1
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
LANGFUSE_HOST=https://cloud.langfuse.com
# Explicit opt-in. Raw prompts/metadata are redacted before any network export.
DEALIX_LANGFUSE_EXPORT_ENABLED=false
# Exact HTTPS egress hosts only; comma-separated. Default keeps Langfuse Cloud only.
DEALIX_LANGFUSE_ALLOWED_HOSTS=cloud.langfuse.com

# Optional MCP HTTP server (STDIO/loopback need none of these).
# Remote binding stays blocked unless both switches are explicitly true.
DEALIX_MCP_REMOTE_HTTP_ENABLED=false
# Set true only when the upstream ingress actually enforces authentication.
DEALIX_MCP_TRUSTED_INGRESS_AUTH=false
# Exact values only; comma-separated; no wildcards.
DEALIX_MCP_ALLOWED_HOSTS=
DEALIX_MCP_ALLOWED_ORIGINS=

# ============================================================
# OTHER
# ============================================================
CLICKBANK_API_KEY=
HIX_AI_API_KEY=
