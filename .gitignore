# ─────────────────────────────────────────────────────────────
# 🔒 SECRETS — NEVER COMMIT
# ─────────────────────────────────────────────────────────────
.mcp.json
.env
.env.*
dealix/transformation/kpi_founder_commercial_import.yaml
dealix/config/founder_closure_intake.yaml
!.env.example
!.env.staging.example
!.env.railway.example
!frontend/.env.local.example
*.pem
*.key
*.p12
*.pfx
secrets/
credentials/
*_secret*
*_credentials*
# Wave 8 exceptions — these are scripts/tests, not credential files
!scripts/dealix_customer_credentials_check.py
!tests/test_wave8_customer_credentials_check.py
!docs/WAVE8_CUSTOMER_CREDENTIAL_READINESS.md
!docs/wave8/customer_credentials.example.env
# The secret scanner itself — code, not a secret (CI: "V3 — No secrets check")
!scripts/check_no_secrets.py
service-account*.json
google-credentials*.json

# ─────────────────────────────────────────────────────────────
# 🐍 Python
# ─────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
!apps/web/lib/
!apps/web/lib/**
!scripts/lib/
!scripts/lib/**
!frontend/src/lib/
!frontend/src/lib/**
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments
venv/
env/
ENV/
.venv/
.env/

# PyInstaller
*.manifest
*.spec

# Testing
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.nox/
coverage.xml
*.cover
*.py,cover
.hypothesis/
nosetests.xml

# Type checkers / linters
.mypy_cache/
.dmypy.json
dmypy.json
.pyre/
.pytype/
.ruff_cache/

# Jupyter
.ipynb_checkpoints
*/.ipynb_checkpoints/*
profile_default/
ipython_config.py

# ─────────────────────────────────────────────────────────────
# 🗄️ Databases
# ─────────────────────────────────────────────────────────────
*.db
*.sqlite
*.sqlite3
*.db-journal
# data/ ignored except for canonical product data (workflows/) — see below
data/*
dumps/
# Service Activation Console data (generated, but committed for static hosting)
!landing/assets/data/
!landing/assets/data/service-readiness.json
# Wave 12.5 §33.2.3 — canonical workflow YAMLs (product data, not customer data)
!data/workflows/
# Demo data — synthetic only, no PII, committed for landing page + CI demos
!data/demo/
# Canonical demo data for PDPA data-flow register (synthetic, no PII)
!data/privacy/
!data/privacy/data_flow_register_seed.json
# Commercial templates — bilingual AR+EN, no customer PII
!data/templates/
# Commercial product data — pain→offer map, catalog, ICP, pricing rules
# (canonical product config, no customer PII)
!data/commercial/
!data/commercial/*
# Governed Commercial Intelligence seeds — policy/config only, never tenant data
!data/commercial_intelligence/
!data/commercial_intelligence/*
# Outreach kit — sector pitches + intake template (NOT the real filled list)
!data/outreach/
!data/outreach/*
# Sector targets — synthetic/public company names, no customer PII
!data/targets/
# Daily draft output — DRAFT_ONLY, no customer PII, founder review
!data/daily_drafts/
!data/leads/.gitkeep

# ─────────────────────────────────────────────────────────────
# 📝 Logs
# ─────────────────────────────────────────────────────────────
logs/
*.log
!docs/transformation/evidence/weekly_ops_checklist.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ─────────────────────────────────────────────────────────────
# 🐳 Docker
# ─────────────────────────────────────────────────────────────
.docker/
docker-compose.override.yml

# ─────────────────────────────────────────────────────────────
# 💻 IDE / Editors
# ─────────────────────────────────────────────────────────────
.vscode/
!.vscode/settings.json.example
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db
.project
.pydevproject
.settings/

# ─────────────────────────────────────────────────────────────
# 🏗️ Build / Dist
# ─────────────────────────────────────────────────────────────
node_modules/
.next/
out/
.cache/
*.tsbuildinfo

# ─────────────────────────────────────────────────────────────
# 🧪 Temporary
# ─────────────────────────────────────────────────────────────
tmp/
temp/
*.tmp
*.bak
*.orig
scratch/
playground/

# ─────────────────────────────────────────────────────────────
# 📊 Analytics / Traces
# ─────────────────────────────────────────────────────────────
.langfuse/
traces/

# Local Dealix index output
.dealix/

# ─────────────────────────────────────────────────────────────
# 🤖 Claude Code agent worktrees + runtime ledger artifacts
# ─────────────────────────────────────────────────────────────
.claude/*
# Project rules and agent definitions are part of the codebase
!.claude/rules/
!.claude/rules/**
!.claude/agents/
!.claude/agents/**
# Agent skills are part of the codebase (SKILL.md + references, no runtime output)
!.claude/skills/
!.claude/skills/**

# Proof ledger runtime artifacts (template + schema + readme stay tracked)
docs/proof-events/*.jsonl
docs/proof-events/*-units.jsonl

# ─────────────────────────────────────────────────────────────
# 🎨 DesignOps OS — local-only artifact exports
# (exporter writes here; never commit founder/customer artifacts)
# ─────────────────────────────────────────────────────────────
docs/designops/exports/**

# ─────────────────────────────────────────────────────────────
# V11 Phase E — live customer board / diagnostics (NEVER commit
# real customer data; the founder's working state lives here)
# ─────────────────────────────────────────────────────────────
docs/phase-e/live/**
docs/diagnostics/**

# ─────────────────────────────────────────────────────────────
# Wave 6 Revenue Activation — live prospect intake / pilot brief /
# payment state / delivery session / proof pack / demo outcomes.
# NEVER commit real prospect or customer data. Founder's working
# state during demo→pilot→delivery lives here.
# ─────────────────────────────────────────────────────────────
docs/wave6/live/**
data/customers/**

# ─────────────────────────────────────────────────────────────
# Wave 8 — Customer-Ready Gate — runtime artifacts (NEVER commit)
# ─────────────────────────────────────────────────────────────
data/customers/**
docs/wave8/live/**
# Customer-specific files (generated by onboarding wizard)
customer_portal_token.txt
env_vars_railway.txt
feature_requests.jsonl
# Observability runtime data
.otel/
.langfuse-local/
# Wave 8 signal synthesis output
signals_output.json

# ─────────────────────────────────────────────────────────────
# Wave 11 — First 3 Paid Pilots Closure — runtime state (NEVER commit)
# ─────────────────────────────────────────────────────────────
data/wave11/**
docs/wave11/live/**
.aider*

# ===================== DEALIX FOUNDER OS MANAGED BLOCK v5 =====================

# Python
.venv/
venv/
env/
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Node / web
node_modules/
.next/
.nuxt/
dist/
build/
coverage/
.cache/
.parcel-cache/
.turbo/
.vercel/
.netlify/

# Aider / AI
.aider.chat.history.md
.aider.input.history
.aider.tags.cache*/
.aider.repo.map
*.aider.log

# Dealix runtime
.dealix/cache/
.dealix/tmp/
.dealix/runs/
.dealix/watchdog/*.pid
.dealix/watchdog/*.lock

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Env
.env
.env.*
!.env.example
!frontend/.env.local.example

# Editors / OS
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db

# Heavy generated artifacts
*.zip
*.tar
*.tar.gz
*.rar
*.7z
*.sqlite
*.db
*.parquet
*.feather
*.mp4
*.mov
*.avi
*.mkv
*.psd
*.fig

# Frozen / legacy areas
dealix-archive/
archive/
legacy/
old/
tmp/

# =================== END DEALIX FOUNDER OS MANAGED BLOCK v5 ===================

# Dealix local-only worktrees
dealix-1.worktrees/
dealix-1/
.aider.tags.cache*/
.aider.chat.history.md
.venv/
node_modules/
dist/
build/
logs/
*.log

# Railway CLI local link
.railway/

# Dealix generated daily operating outputs
company/runtime/
company/outbox/*_master_stable_approval_queue.csv
company/outbox/*_micro_master_approval_queue.csv
company/reports/*_MASTER_STABLE_CEO_REPORT.md
company/reports/*_MICRO_MASTER_CEO_REPORT.md

# Dealix intake generated outputs
company/runtime/intake/

# Dealix outreach — real target list stays local (never committed)
data/outreach/saudi_target_intake.csv
# Real outreach log stays local; the .template.csv is tracked (allowlisted above).
data/outreach/outreach_log.csv
reports/outreach/
reports/command_room/

# Researched raw target lists stay local (not a committed scraped list)
data/outreach/research/
reports/content/

# GTM generated docs — founder reviews before sharing; never committed
reports/contracts/*
reports/pilot_reports/*
reports/customer_reports/*
reports/meetings/*
!reports/contracts/.gitkeep
!reports/pilot_reports/.gitkeep
!reports/customer_reports/.gitkeep
!reports/meetings/.gitkeep

# Contract log is tracked business data (like proposals/)
# data/contracts/contract_log.csv is NOT ignored

# Dealix real leads generated outputs
company/runtime/places/
dealix_local_test.db
reports/runtime/
reports/daily_intelligence/
reports/github-context/
ledgers/private/
.env.production

# Startup release gate + commercial OS runtime reports (generated; never commit)
reports/startup_release_gate/
reports/startup_command_center/
reports/startup_proof_pack/
reports/founder_daily_brief/
reports/founder_growth_os/
reports/growth_command_center/
reports/commercial_launch_control/
reports/agentic_commercial_os/
reports/agents/
reports/ai/
reports/commercial/
reports/operator/
reports/command_center/

# Daily business exports (founder reviews before sharing; never committed)
business/closing/exports/
business/crm/exports/dealix-pipeline-report-*.md
business/demo/exports/
business/persuasion/exports/
business/proposals/generated/
business/reports/exports/

# Generated server env / surface reports (daily; not committed)
reports/server/env_contract.md
reports/server/public_surfaces.json

# Generated outreach batch files (CSV leads)
data/outreach/ready_batch_*.csv

# Company directory targeting artifacts stay local. The source terms for the
# provided directory are unverified and this repository is public. Commit the
# importer, schemas, tests, and runbooks — never the derived company dataset.
outputs/company_targeting/
reports/company_targeting/
reports/company_launch/
reports/revenue/*/daily_ceo_report.json
reports/revenue/*/daily_ceo_report.md
reports/revenue/*/score_summary.json

# ─────────────────────────────────────────────────────────────
# 💼 Commercial Package Archives — never commit bundles
# ─────────────────────────────────────────────────────────────
*.zip
*.tar.xz
*.tar.gz
*.rar
*.7z
*_BUNDLE*
*_EXPORT*
*_PATCH*
*_PACK*

# ─────────────────────────────────────────────────────────────
# 📝 Session export artifacts
# ─────────────────────────────────────────────────────────────
SESSION_COMMIT_HISTORY.txt
SESSION_COMPLETION_SUMMARY.md
PR_DESCRIPTION_*.md

