# Agents Runtime Scope

This directory is reserved for agent runtime boundaries and operating contracts.

Current priority:

- keep agents workflow-bound (not free-form autonomous)
- require governance gates before any external action
- route all Revenue OS activity through the first unavoidable workflow
