# Agents Layer

This directory defines governed agent specifications.

Current scope is intentionally narrow:

- One agent only: `sales_agent`

Each agent folder must include:

- executable spec (`agent.yaml`)
- permissions
- tools contract
- eval specification
- risk profile
