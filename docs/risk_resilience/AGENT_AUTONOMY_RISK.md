# Agent Autonomy Risk

Threats: agent acts outside purpose, uses wrong tool, requests broader permissions, performs external action, mixes client contexts.

Controls: Agent Card, Agent Registry, Autonomy Levels (MVP 0–3), Allowed Tools, Forbidden Actions, Audit Log, Approval Queue, Decommission Rule.
