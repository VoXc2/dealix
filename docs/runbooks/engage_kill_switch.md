# Runbook: Engage Kill Switch

1. Identify `tenant_id`, `agent_id`, and reason.
2. Engage kill switch in runtime safety layer.
3. Confirm agent status becomes `isolated`.
4. If a run is active, confirm run state becomes `paused`.
5. Open incident record with trace references.
