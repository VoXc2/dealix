# تنفيذ 4 أسابيع — من Service Kit إلى أول عميل

## الأسبوع الأول: Service Operating Kit

ابنِ:

- `docs/company/` + `docs/services/` (ثلاثة ركائز: Lead Intelligence، Quick Win، Company Brain)
- `docs/quality/` + `docs/governance/`
- `docs/templates/`

جهّز: عروض، نطاق، intake، QA، proof pack، **sample reports** (انظر `sample_output.md` في كل خدمة).

**هدف الأسبوع:** تشرح وتبيع باحتراف.

---

## الأسبوع الثاني: Delivery MVP

ركّز على:

- `data_os` + `revenue_os` + `governance_os` + `reporting_os` + `delivery_os`

ميزات دنيا: import preview، data quality، service readiness، governance checks، proof pack منطقي، lead scoring حيث ينطبق.

**هدف الأسبوع:** تسليم Lead Intelligence Sprint من طرف إلى طرف (مسودات + تقارير).

---

## الأسبوع الثالث: Demos

ثلاثة عروض: Lead Intelligence (CSV + تقرير + مسودات)، Quick Win (خريطة عملية + workflow)، Company Brain (عيّنة ملفات + إجابات بمصادر + مثال insufficient evidence).

**هدف الأسبوع:** إقناع بصري وعملي.

---

## الأسبوع الرابع: Sales

40 شركة مقسّمة (20 B2B services، 10 عيادات، 10 لوجستيات/تعليم/عقار).

أهداف كمية مقترحة: 8 ردود، 5 مكالمات، 2 عروض، **1 عميل مدفوع**.

استخدم: [`../sales/SALES_SCRIPT_AR.md`](../sales/SALES_SCRIPT_AR.md).
