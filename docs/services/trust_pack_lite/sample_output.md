# Sample output (ILLUSTRATIVE — not a real client result)

This is a structural illustration only. It uses no real client data.

- AI action policy: e.g. "agents draft only; humans approve sends".
- Approval matrix: 3 illustrative rows.
- Forbidden actions: e.g. "no autonomous external send", "no un-sourced claims".
- Agent safety rules: placeholder identity and scope-limit lines.

Replace with an anonymized real example only after a first paid delivery.

Estimated value is not Verified value / القيمة التقديرية ليست قيمة مُتحقَّقة
