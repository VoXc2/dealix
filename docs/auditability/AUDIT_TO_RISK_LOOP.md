# Audit → Risk Loop

Every audit anomaly becomes a risk signal that yields a rule, test, checklist, and dashboard metric.

Examples: output without governance status → QA failure; source without passport → data risk; agent requesting new tool → autonomy risk; approval delay → adoption friction; claim without proof → claim risk.
