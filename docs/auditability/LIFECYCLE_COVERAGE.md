# Lifecycle Coverage

Per agent: created, approved, modified, tool permission changed, run executed, incident, decommissioned.

Per workflow: created, owner assigned, source attached, AI run executed, output reviewed, approval, proof, retainer recommendation.
