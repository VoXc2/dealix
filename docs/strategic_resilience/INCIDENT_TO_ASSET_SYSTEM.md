# Incident → Asset System

Every incident produces 1 rule + 1 test + 1 checklist update + 1 owner + 1 prevention metric.

Sev-1/Sev-2 cannot close without change (enforced by `institutional_control_os.incident_response.incident_must_create_change()`).
