# Session Report — TEMPLATE | تقرير الجلسة — قالب

> Copy this file to `session_report_YYYY-MM-DD.md` and fill it from real
> sources only. Leave any field `null` if you have no real source — an
> honest gap beats a fabricated entry (Gate 4: "fill truth, not hope").
>
> انسخ هذا الملف إلى `session_report_YYYY-MM-DD.md` واملأه من مصادر حقيقية
> فقط. اترك أي حقل `null` إذا لم يوجد مصدر — الفراغ الصادق أفضل من رقم مختلَق.

---

## 1. Meta | البيانات الأساسية

| Field | Value |
|-------|-------|
| Date \| التاريخ | `YYYY-MM-DD` |
| Author \| المُعِدّ | `null` |
| Branch / commit \| الفرع | `null` |

## 2. Gate Status | حالة البوابات

| Gate \| البوابة | Status \| الحالة | Note \| ملاحظة |
|------|--------|------|
| 1 — PR closed technically | `pending` | |
| 2 — main verified | `pending` | |
| 3 — repo frozen | `pending` | |
| 4 — truth filled | `pending` | |
| 5 — L4 decision | `pending` | |

## 3. Verification Results | نتائج التحقق

Record the actual exit status of each check that was run.

| Check \| الفحص | Result \| النتيجة |
|------|--------|
| `verify_global_ai_transformation.py` | `not_run` |
| `business_readiness_verify.sh` | `not_run` |
| `check_alembic_single_head.py` | `not_run` |
| `verify_dealix_ready.py` | `not_run` |

## 4. KPI Deltas | تغيّرات المؤشرات

Only list a KPI here if it has a real `source_ref` in
`dealix/registers/kpi_baselines.yaml`. Otherwise leave the table empty.

| KPI \| المؤشر | Previous \| السابق | Current \| الحالي | Source \| المصدر |
|-----|----------|---------|--------|
| _(none — all KPIs still null)_ | | | |

## 5. Market Events | الأحداث السوقية

Record only events that actually happened (a real send, reply, meeting,
invoice). No predictions.

| Event \| الحدث | Audience \| الجمهور | Outcome \| النتيجة | Signal level \| مستوى الإشارة |
|-------|---------|--------|--------|
| _(none recorded)_ | | | |

## 6. Next Action | الإجراء التالي

> One concrete next action. Do not build before a market signal asks for it.
> إجراء واحد محدد. لا تبنِ قبل أن تطلب إشارة سوقية ذلك.

`null`
