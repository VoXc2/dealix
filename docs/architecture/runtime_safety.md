# Runtime Safety

Runtime safety layer provides:

- Kill switch: isolates target agent and can pause active run.
- Circuit breaker: opens after failure threshold.

Safety actions are designed for deterministic containment, not best-effort behavior.
