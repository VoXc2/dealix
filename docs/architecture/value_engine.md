# Value Engine

## Purpose
Internal workflow ROI/value accounting per run and tenant.

## Discipline
- `measured`/`verified` tiers require `source_ref`.
- `client_confirmed` requires `confirmation_ref`.
- ROI report aggregates measured vs total estimated value.
