# Value Engine

`value_engine_os` tracks workflow ROI metrics.

- `estimated` metrics are allowed without external proof.
- `measured` metrics require `source_ref`.
- Metrics can emit control events for audit trace.
- ROI report aggregates measured vs estimated totals per run.
