# Self Evolving — Propose Only

## Rule
Self-evolving proposals are generated as `proposed` artifacts only.

## Apply guard
- `apply` is blocked until explicit approval grant.
- Approved proposal transitions: `proposed -> approved -> applied`.
- No automatic patch apply in autonomous runtime.
