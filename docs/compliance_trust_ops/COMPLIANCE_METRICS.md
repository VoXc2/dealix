# Compliance Metrics

% sources with passport, % AI runs logged, % outputs with governance status, % external actions approved, PII flags, redactions, blocked actions, incidents by severity, time-to-resolve.

Thresholds: <100% AI run logging → no enterprise; <100% passport coverage → no scale; any unapproved external action → incident review.
