# Source Passport v2

Adds `collection_context` and `deletion_required_after` to the canonical schema. Rules unchanged: no passport = no AI; no allowed use = internal only; PII unclear = redact or block; external action = approval required; unknown source = no outreach.
