# Claim Compliance

Forbidden: guaranteed sales, "strongest AI", unverified results, public case studies without consent.

Status: UNSUPPORTED → block; ESTIMATED → caveat; OBSERVED → in Proof Pack; VERIFIED → case-safe summary with permission.

Typed: `compliance_trust_os.claim_compliance.classify_claim_status()` + `can_use_in_marketing()` requires VERIFIED.
