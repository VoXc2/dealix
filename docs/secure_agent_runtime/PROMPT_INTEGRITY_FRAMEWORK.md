# Prompt Integrity Framework

Trusted instruction envelope vs untrusted data envelope. Untrusted data may inform outputs but never override policy.

Typed: `secure_agent_runtime_os.prompt_integrity.PromptEnvelope.can_override_policy()` always returns False.
