# Stateful Risk Memory

Tracks across session: PII exposure, external action attempts, blocked tool count, policy warnings, untrusted context exposure.

Re-derives runtime state. Typed: `secure_agent_runtime_os.risk_memory.update_risk_memory()`.
