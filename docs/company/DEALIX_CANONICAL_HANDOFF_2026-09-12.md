# Dealix Canonical Handoff — 2026-09-12

Status: durable continuity snapshot for humans and agents.  
Captured from current GitHub truth plus previously recorded canonical VPS receipts on 2026-09-12 Riyadh time.  
Git snapshot base: `9c662df714f808b59884aa4f2028545b355aaa65`.

## 1. Truth precedence

When sources conflict, use this order:

1. Fresh live runtime receipts from the canonical VPS.
2. Current exact `origin/main` and exact-head acceptance receipts.
3. Merged PR evidence and current open-PR metadata.
4. Canonical repo policies and operating contracts.
5. Historical transfer files, chats, notes and memory.

Never convert an old PASS, old SHA, HTTP 200, draft, research record, synthetic test, model output or queued job into current production/commercial truth.

## 2. Company mission

Dealix is a Saudi-first AI Business Operating System / autonomous-company operating layer for B2B execution.

North Star:

`CASH_READY_AUTONOMOUS_DEALIX_COMPANY`

Optimize verified economic movement per founder minute while minimizing cost, risk, compute waste and unverified claims.

Founder operating intent:

- low-intervention internal operation;
- aggressive L0-L4 delegation;
- broad Saudi sector coverage;
- continuous research and self-improvement;
- OpenCode/Hermes-assisted engineering and operations;
- one canonical Company Machine rather than disconnected stacks;
- free-first diagnostics and customer-specific commercial proposals.

## 3. Non-negotiable truth firewall

- Research != Relationship.
- Public contact != Consent.
- Signal != Qualified Problem.
- Engagement != Buyer Intent.
- Draft != Sent.
- Account Plan != Proposal.
- Quote != Invoice.
- Invoice != Payment.
- Payment Request != Verified Cash.
- Payment != Revenue unless canonical economic evidence allows recognition.
- Delivery != Customer Value.
- Customer Value != Public Proof.
- Synthetic/Demo != Customer Proof.
- HTTP 200 != Release Identity.
- Merged PR != Deployed Release.
- Historical PASS != Current Exact-Head PASS.
- SKIPPED != PASS.
- Queued != Executed.
- Model Output != Authority.

## 4. Authority

Bounded autonomy remains the operating law:

- L0 Observe
- L1 Analyze
- L2 Draft
- L3 Internal Execute
- L4 Repository Execute
- L5 Material External Effect

L5 includes live customer sends, public publishing, production deploy/cutover, DNS, production DB/schema, secrets/keys, firewall/root infrastructure mutation, payment/spend, contract acceptance and tender submission.

The founder explicitly authorized the repository convergence/merge work represented by the merged chain below. That authorization does not silently authorize unrelated production or commercial external effects.

Communications law remains:

- WhatsApp inbound/opt-in only; no cold WhatsApp mass automation.
- No personal LinkedIn scraping or mass automation.
- Gmail/customer communications may be drafted internally; sending is authority-bound.
- Slack/Telegram can operate as governed internal control surfaces.

## 5. Adopted company architecture

Merged PR `#1731` adopts the new target architecture:

`Dealix Holding -> Sector Companies -> Arm Pods -> Specialist Agents`

Key law:

- one Company Brain;
- one Opportunity/Economic truth graph;
- one approval system;
- one scheduler/orchestration fabric;
- one model-routing authority;
- one proof/economic truth system;
- many logical specialist agents;
- bounded active runtime workers.

Logical agent count and runtime concurrency are separate concepts. Dealix may define hundreds or thousands of logical specialists without spawning a permanent process for each one.

The historical exactly-five-agent and global `DEEP_WIP_MAX=3` assumptions are now migration/compatibility constraints, not the target company constitution. Their replacement must be deliberate, tested and resource-governed rather than bypassed.

## 6. Commercial execution law

Canonical ladder:

`Market Signal -> Evidence -> Real Interaction -> Qualified Problem -> Free Diagnostic -> Discovery -> Customer-Specific Solution/Quote -> Pilot Decision -> Invoice -> Verified Payment -> Delivery -> Customer-Validated Proof -> Expansion / Referral / Productization`

Research-only company/account records must never manufacture relationship, consent, pipeline or revenue.

All diagnostics remain free/card-free unless a later explicit commercial policy supersedes that rule.

## 7. Current merged convergence chain

At this snapshot, the important merged sequence includes:

- `#1723` — autonomy, commercial launch gates and governed software-evolution convergence.
- `#1725` — Saudi Opportunity Radar and governed commercial routing on the live `apps/web` surface.
- `#1729` — transitive `fflate` security remediation.
- `#1730` — truth-safe real-company radar and internal hypothesis-only account plans.
- `#1731` — Agentic Holding / Sector Company / Arm Pod architecture and bounded OpenCode launcher.
- `#1732` — Software Acquisition/Evolution V2 hardening without runtime coupling.
- `#1728` — OpenClaw Jobs compaction recovery consolidated on current main.

Exact Git snapshot after `#1728`:

`main = 9c662df714f808b59884aa4f2028545b355aaa65`

### Acceptance evidence retained

`#1730` exact-head verification before merge:

- focused pytest: 5 passed;
- Ruff: PASS;
- Python compile: PASS;
- `git diff --check`: PASS.

`#1732` exact-head verification before merge:

- focused pytest: 28 passed;
- Ruff: PASS;
- Python compile: PASS;
- diff-check: PASS.

`#1728` merged receipt records:

- Bash syntax: PASS;
- diff-check: PASS;
- exact remote-head parity: PASS;
- live VPS proof: effective 32K context, 20K reserve floor, local-first founder route, healthy loopback Gateway RPC and working Telegram in the checked post-repair window.

These receipts do not imply production release parity for unrelated services.

## 8. Website truth

The live development surface is `apps/web`.

Older `frontend/` is historical/frozen unless a current, explicitly accepted migration proves unique value that still belongs in the live surface.

Merged `#1725` added `/saudi-opportunity-radar` with official-source-first Saudi signals and free-diagnostic routing. Its public truth boundary remains:

`Public Signal != Buyer Intent != Relationship != Consent != Pipeline != Revenue`

The canonical client API already exposes `POST /api/v1/public/leads` through `apps/web/lib/api.ts`; do not revive an old frontend lane solely to reimplement this behavior.

Production Green remains a separate exact-release claim requiring fresh release identity, front-door, health/smoke and provider parity evidence.

## 9. Real-company intelligence

Merged `#1730` provides a truth-safe company research layer:

- authoritative-source identity dedupe;
- research-only records;
- internal hypothesis-only account plans;
- customer-specific proposal gate that fails closed without real interaction / qualified-problem evidence;
- initial Saudi finance/fintech and real-estate seed records.

This layer feeds the canonical Opportunity Graph. It is not a second CRM and does not grant consent or pipeline state.

## 10. Software acquisition and self-evolution

Merged `#1732` strengthens the canonical Software Acquisition & Evolution Factory with:

- provenance/evidence gates;
- capability/blast-radius mapping;
- structural sandbox requirements;
- KEV/EPSS/SBOM/rollback controls;
- stricter 7-day and 30-day promotion decisions;
- public-surface drift and required-HTTP verification.

Internet discovery remains evidence, not trust. No blind `curl | sudo bash`, privileged host install, duplicate Company Machine or uncontrolled paid model spill.

## 11. OpenClaw / Telegram / local AI

Merged `#1728` consolidates the OpenClaw compaction recovery source and records live VPS evidence from the repair window.

The repair aligns effective Ollama context, compaction reserve, founder local-first routing and gateway/session recovery. Its canaries use local/non-delivered execution paths; the source must not be treated as permission for unrelated runtime mutations.

Keep local model endpoints private. Prefer local/free/included models when quality is sufficient. Paid spill remains disabled by default unless separately governed.

## 12. Runtime compatibility debt

Some runtime scripts still encode historical constants such as the five-agent ownership model and `DEEP_WIP_MAX=3`.

Do not interpret those as a reason to undo `#1731`. The required migration path is:

1. introduce hierarchical Agent/Sector/Arm registries and compatibility adapters;
2. migrate authority and ownership lookup;
3. introduce resource-aware concurrency budgets;
4. preserve worktree isolation for parallel writers;
5. migrate legacy tests/invariants;
6. run exact-head acceptance before removing compatibility facades.

No second scheduler, Company Brain, CRM, economic ledger or model router may be created during this migration.

## 13. Founder control loop

Desired interface:

`Telegram DM -> OpenClaw/Hermes -> Canonical Company Machine -> Receipts -> Founder Brief`

Founder brief should prioritize:

- MONEY
- DECISIONS
- RISKS
- APPROVALS
- NEXT_ACTION

Agents should execute safe internal work rather than merely proposing it, and every meaningful execution lane should emit evidence-backed receipts.

## 14. Immediate continuation order

1. Fetch current `origin/main` before every new lane; this snapshot SHA is not permanently current.
2. Implement the `#1731` hierarchy migration incrementally on top of the canonical Company Machine.
3. Replace legacy fixed agent/WIP assumptions with resource-aware governance only after tests prove parity and safety.
4. Keep `apps/web` as the live public surface; port only genuinely unique value from historical frontend work.
5. Continue Saudi official-source intelligence -> free diagnostics -> real interaction -> qualified discovery without inflating commercial truth.
6. Run Software Evolution V2 through the existing Session Factory/OpenCode path; no parallel acquisition stack.
7. Verify production release identity separately before any Production Green claim.
8. Preserve WhatsApp opt-in/inbound law, local-model privacy, Economic Truth and exact-action authority gates.
9. Diagnose failed Session Factory jobs from exact receipts rather than resubmitting duplicates blindly.
10. Maintain one canonical handoff/checkpoint after material convergence so future agents continue instead of restarting.

## 15. Supersession policy

Old PRs, scripts and documents are not merged merely because they contain useful historical work.

For every stale lane:

- compare against current main;
- retain only unique non-duplicate value;
- port it into the canonical architecture if still useful;
- close the stale lane as superseded;
- never reintroduce obsolete invariants or parallel stacks.

This file is a continuity checkpoint, not an authorization token and not a substitute for live verification.
