# LLM Gateway

Model Router + Prompt Registry + Schema Validator + Redaction + Cost Guard + Eval Hook + AI Run Ledger.

No AI call outside gateway. No prompt outside registry. No client-facing output without schema + governance + QA.
