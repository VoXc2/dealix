---
title: Skills
description: Reusable agent skills catalogue. Loaded from skills/MANIFEST.yaml and exposed both as REST endpoints and as MCP tools to Claude Desktop.
---

## List the catalogue

```http
GET /api/v1/skills
```

Returns every skill registered in `skills/MANIFEST.yaml`. Each entry
declares its `id`, source `path`, human-readable `description`, the
list of `inputs` it accepts, and its `output_shape`.

```json
{
  "count": 12,
  "skills": [
    {
      "id": "sales_qualifier",
      "path": "skills/sales-qualifier/SKILL.md",
      "description": "Score a lead against BANT + Saudi PDPL contactability gate.",
      "inputs": ["full_name", "company", "phone", "email", "sector"],
      "output_shape": "{score:0..1, gates:{bant,pdpl}, reasoning:string}"
    }
  ]
}
```

## Read one skill

```http
GET /api/v1/skills/{id}
```

Returns 404 `skill_not_found` for unknown ids. The 12 canonical ids
shipped today:

- `sales_qualifier`
- `proposal_writer`
- `meeting_summarizer`
- `crm_syncer`
- `email_triage`
- `compliance_reviewer`
- `contract_analyst`
- `market_researcher`
- `lead_scorer`
- `renewal_forecaster`
- `content_generator_ar`
- `ar_en_translator`

## MCP exposure

Every skill is auto-registered as a Model Context Protocol tool by
`dealix/mcp/server.py`. A customer adding Dealix as an MCP server in
Claude Desktop sees the catalogue as native tools — they call
`proposal_writer(lead_id=...)` without importing the Dealix SDK.

## Adding a skill

1. Create `skills/<slug>/SKILL.md` in Anthropic Skills format.
2. Create `skills/<slug>/prompt.yaml` with a Jinja-template prompt.
3. Append an entry to `skills/MANIFEST.yaml`.
4. Optional: register a Python handler via `@register("<id>")` in
   `dealix/agents/skills/handlers.py` (T8a) or a themed file like
   `handlers_llm.py` / `handlers_data.py` (T9).
5. Restart — both `/api/v1/skills` and the MCP server pick it up.

## Handlers shipped today

| Skill id | Engine | Source |
| --- | --- | --- |
| `sales_qualifier` | BANT × PDPL gate | `handlers.py` (T8a) |
| `lead_scorer` | tunable weighted score | `handlers.py` (T8a) |
| `content_generator_ar` | Saudi-tone copy | `handlers.py` (T8a) |
| `ar_en_translator` | glossary-aware bidirectional | `handlers.py` (T8a) |
| `proposal_writer` | SAR + ZATCA-aware proposal | `handlers_llm.py` (T9) |
| `email_triage` | 13-bucket AR + EN classifier | `handlers_llm.py` (T9) |
| `contract_analyst` | risk-flag + redline extractor | `handlers_llm.py` (T9) |
| `meeting_summarizer` | transcript → summary + actions | `handlers_llm.py` (T9) |
| `crm_syncer` | HubSpot bidir + Salesforce stub | `handlers_data.py` (T9) |
| `market_researcher` | Wathq/Maroof/Najiz/Tadawul/MISA | `handlers_data.py` (T9) |
| `renewal_forecaster` | health × usage × NPS → churn | `handlers_data.py` (T9) |
| `compliance_reviewer` | PDPL + GDPR + CITC pass | `handlers_data.py` (T9) |

Every handler degrades to a deterministic rule-based path when the
relevant vendor key is unset — so unit-testing is cheap and pilot
deployments work without external dependencies.
