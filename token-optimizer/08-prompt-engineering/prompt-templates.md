# قوالب برومبت جاهزة للمهام الشائعة

## إضافة Feature جديد

```
أضف [الميزة] إلى `[ملف]`.

المطلوب:
- [متطلب 1]
- [متطلب 2]

القيود:
- [قيد 1]

لا شرح. الكود فقط. أضف فقط التغييرات الضرورية.
```

---

## إصلاح Bug

```
في `[ملف]`، السطر [X-Y]:
[وصف المشكلة بجملة واحدة]

السلوك المطلوب: [ما يجب أن يحدث]
السلوك الحالي: [ما يحدث الآن]

أعطني الدالة المُصلحة فقط. لا شرح.
```

---

## مراجعة كود

```
راجع `[ملف]` وأعطني:
- مشاكل حقيقية فقط (لا تُشير للكود الجيد)
- كل مشكلة: [سطر] → [المشكلة] → [الحل]

لا شرح مُطوَّل. قائمة نقاط فقط.
```

---

## كتابة اختبارات

```
اكتب اختبارات لـ `[ملف:دالة]`.
حالات الاختبار:
1. [حالة نجاح]
2. [حالة فشل]
3. [حالة edge case]

استخدم نفس أسلوب `tests/conftest.py`.
لا شرح. فقط الكود.
```

---

## Refactor محدود

```
في `[ملف]` دالة `[اسم الدالة]` (السطر [X-Y]):
أعد هيكلتها لـ [الهدف].

قيود:
- لا تُغير الـ interface الخارجي
- لا تُغير أي ملفات أخرى

أعطني الدالة المُعاد هيكلتها فقط. بأسلوب diff.
```

---

## إضافة Validation

```
أضف validation لـ `[field]` في `[schema]`.
القاعدة: [وصف القاعدة بدقة]
رسالة الخطأ: "[نص الخطأ]"

Pydantic v2 syntax. لا شرح.
```

---

## ترجمة / تعريب

```
في `[ملف]`:
أضف نسخة عربية لكل النصوص التالية:
[قائمة النصوص]

اتبع نمط المشروع: field_name → field_name_ar
لا شرح. الكود فقط.
```

---

## طلب ملخص كود

```
لخّص `[ملف]` في:
- الغرض: [جملة واحدة]
- الدوال الرئيسية: [قائمة بأسمائها فقط]
- التبعيات: [imports مهمة]

5 سطور كحد أقصى.
```

---

## التعامل مع Migrations

```
أنشئ migration لـ:
- جدول: [اسم]
- الأعمدة: [قائمة]
- القيود: [قائمة]

استخدم: `alembic revision --autogenerate -m "[وصف]"`
لا شرح. فقط محتوى ملف الـ migration.
```
