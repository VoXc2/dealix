# Trust Pack — `<PARTNER>` × Dealix

> Fill in every `<TOKEN>` below. Remove this banner before sharing.

## Who We Are

`<PARTNER>` is co-selling Dealix's Governed AI Operations services in
`<COUNTRY>` under the open Governed AI Operations Doctrine
(`https://dealix.example.com/api/v1/doctrine`).

## The Eleven Non-Negotiables We Adopt

1. Source Passport before AI use.
2. Human approval before external action.
3. Governance Runtime before client-facing output.
4. Proof Pack before any claim.
5. No scraping.
6. No cold WhatsApp.
7. No LinkedIn automation.
8. No guaranteed sales claims.
9. No agent without identity.
10. Capital Asset registration before invoice.
11. Verifiable, not merely trusted.

Each is locked by a public test in the Dealix repository. See
`https://dealix.example.com/api/v1/dealix-promise`.

## Our Data Boundary

- Customer-owned / customer-introduced data only.
- No cross-border transfer of personal data without explicit consent
  and a documented lawful basis.
- Residency: `<JURISDICTION>` (e.g., Saudi Arabia).

## Our Governance Boundary

- Every external action by an AI passes a human approval gate.
- Every output destined for a client passes Dealix's Governance Runtime
  (decision: ALLOW / DRAFT_ONLY / REQUIRE_APPROVAL / REDACT / BLOCK /
  ESCALATE).

## Our Proof Discipline

- No claim is published without a Proof Pack.
- No case study is shared without explicit client approval.

## Public Verification

| What                       | Where                                              |
|----------------------------|----------------------------------------------------|
| 11 non-negotiables         | `GET /api/v1/dealix-promise`                       |
| Doctrine snapshot          | `GET /api/v1/doctrine` (and `?version=`)           |
| Per-customer readiness     | `GET /api/v1/customer/{handle}/readiness/public`   |
| Capital Asset count (safe) | `GET /api/v1/capital-assets/public`                |
| Trust status (Shields.io)  | `GET /api/v1/trust/status`                         |

## Sign-off

Signed: `<PARTNER REPRESENTATIVE>`
Date:   `<YYYY-MM-DD>`
Doctrine version adopted: `v1.0.0`
