# Workflows Layer

This directory contains operational workflows that connect trigger -> steps -> approval -> execution -> metrics.

Current production target:

- `sales/lead_qualification.workflow.yaml`

Guideline:

- Build one workflow deeply before adding more workflows.
